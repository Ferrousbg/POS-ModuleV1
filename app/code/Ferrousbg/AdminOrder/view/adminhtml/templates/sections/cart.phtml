<?php /** @var \Magento\Backend\Block\Template $block */ ?>
<div class="h-full flex flex-col p-6">
    <h2 class="text-2xl font-black mb-4 text-slate-800 border-b border-slate-100 pb-4 flex justify-between items-center">
        <span class="flex items-center gap-2"><span class="text-orange-500">🛒</span> Cart</span>
        <span class="text-orange-600 font-bold text-base bg-orange-50 border border-orange-100 px-3 py-1 rounded-lg" x-text="cart.length + ' items'"></span>
    </h2>

    <div class="flex-1 overflow-y-auto pr-2 custom-scroll">
        <template x-for="(item, index) in cart" :key="index">
            <div class="flex justify-between items-center border-b border-slate-100 py-5 hover:bg-slate-50 transition-colors px-2 rounded-xl group">
                <div class="flex-1 pr-4">
                    <div class="text-xl font-bold text-slate-800 leading-tight mb-1" x-text="item.name"></div>
                    <div class="text-base text-slate-400 font-mono" x-text="item.sku"></div>
                </div>

                <div class="flex items-center gap-6">
                    <!-- Qty Control (Orange hover) -->
                    <div class="flex items-center border border-slate-200 rounded-xl overflow-hidden bg-white shadow-sm">
                        <button @click="item.qty > 1 ? (item.qty--, calculateTotal()) : removeFromCart(index)"
                                class="w-12 h-10 flex items-center justify-center hover:bg-orange-50 hover:text-orange-600 text-xl font-bold text-slate-500 transition">-</button>

                        <input type="number" x-model="item.qty" @change="calculateTotal()"
                               class="w-14 h-10 text-center border-x border-slate-100 p-0 text-xl font-bold text-slate-800 focus:ring-0">

                        <button @click="item.qty++; calculateTotal()"
                                class="w-12 h-10 flex items-center justify-center hover:bg-orange-50 hover:text-orange-600 text-xl font-bold text-slate-500 transition">+</button>
                    </div>

                    <div class="text-right w-32 font-bold text-slate-800 text-2xl" x-text="formatPrice(item.price * item.qty)"></div>

                    <button @click="removeFromCart(index)" class="text-slate-300 hover:text-red-500 transition p-2">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
                    </button>
                </div>
            </div>
        </template>

        <div x-show="cart.length === 0" class="h-full flex flex-col items-center justify-center text-slate-300">
            <svg class="w-24 h-24 mb-4 opacity-20" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path></svg>
            <p class="text-2xl font-bold">Cart is empty</p>
            <p class="text-base">Scan items to begin</p>
        </div>
    </div>

    <!-- Totals & Actions -->
    <div class="mt-4 pt-6 border-t border-slate-100 bg-white">
        <div class="flex justify-between items-end mb-6">
            <span class="text-xl font-bold text-slate-500">Total Amount:</span>
            <span class="text-5xl font-black text-slate-800" x-text="formatPrice(grandTotal)"></span>
        </div>

        <!-- MAIN ACTION BUTTON: GREEN (Standard for GO) or ORANGE?
             Let's use a very deep Orange/Red for action to fit the theme -->
        <button @click="placeOrder()"
                :disabled="!isValidOrder"
                :class="{'opacity-50 cursor-not-allowed bg-slate-300': !isValidOrder, 'bg-orange-600 hover:bg-orange-700 shadow-xl shadow-orange-600/30 transform active:scale-[0.99]': isValidOrder}"
                class="w-full text-white font-black py-5 rounded-2xl transition text-2xl flex justify-center items-center gap-3">
            <span x-show="!placingOrder">🚀 Complete Order</span>
            <span x-show="placingOrder" class="flex items-center gap-2">Processing...</span>
        </button>

        <div x-show="!isValidOrder" class="text-center text-orange-600 mt-3 font-bold text-base bg-orange-50 py-3 rounded-xl border border-orange-100">
            Select Customer & Add Products to continue
        </div>
    </div>
</div>